import { tcv } from "@choice-ui/shared"

export const hexInputTv = tcv({
  base: [
    "h-6 min-w-0 px-2",
    "disabled:text-disabled-foreground",
    "cursor-default bg-transparent",
    "appearance-none",
  ],
})
